#pragma once

namespace SynthUtils {
    enum class WaveformType {
        Sine,
        Square,
        Sawtooth,
        Triangle,
    };

    enum class Note {
      C, CS, D, DS, E, F, FS, G, GS, A, AS, B, Count, None
    };
}
